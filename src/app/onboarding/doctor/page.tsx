"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
export default function DoctorOnboardingPage(){const r=useRouter();const [form,setForm]=useState({fullName:"",specialization:"",hospital:"",yearsOfExperience:"",licenseId:""});const [err,setErr]=useState<string|null>(null);async function submit(){setErr(null);const res=await fetch('/api/onboarding/complete',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,role:'doctor'})});const data=await res.json();if(!res.ok||!data.ok){setErr(data.error??'Save failed');return;}r.replace('/doctor');}
return <main className='mx-auto max-w-xl py-12 space-y-3'><h1 className='text-2xl'>Doctor onboarding</h1><input placeholder='fullName' className='border p-2 w-full' onChange={e=>setForm({...form,fullName:e.target.value})}/><input placeholder='specialization' className='border p-2 w-full' onChange={e=>setForm({...form,specialization:e.target.value})}/><input placeholder='hospital' className='border p-2 w-full' onChange={e=>setForm({...form,hospital:e.target.value})}/><input placeholder='yearsOfExperience' className='border p-2 w-full' onChange={e=>setForm({...form,yearsOfExperience:e.target.value})}/><input placeholder='licenseId' className='border p-2 w-full' onChange={e=>setForm({...form,licenseId:e.target.value})}/><button onClick={submit} className='bg-black text-white p-3 rounded'>Save and continue</button>{err&&<p>{err}</p>}</main>}
