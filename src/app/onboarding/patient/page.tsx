"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
export default function PatientOnboardingPage(){const r=useRouter();const [form,setForm]=useState({fullName:"",age:"",gender:"",phone:"",medicalNotes:""});const [err,setErr]=useState<string|null>(null);async function submit(){setErr(null);const res=await fetch('/api/onboarding/complete',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,role:'patient'})});const data=await res.json();if(!res.ok||!data.ok){setErr(data.error??'Save failed');return;}r.replace('/patient');}
return <main className='mx-auto max-w-xl py-12 space-y-3'><h1 className='text-2xl'>Patient onboarding</h1><input placeholder='fullName' className='border p-2 w-full' onChange={e=>setForm({...form,fullName:e.target.value})}/><input placeholder='age' className='border p-2 w-full' onChange={e=>setForm({...form,age:e.target.value})}/><input placeholder='gender' className='border p-2 w-full' onChange={e=>setForm({...form,gender:e.target.value})}/><input placeholder='phone' className='border p-2 w-full' onChange={e=>setForm({...form,phone:e.target.value})}/><textarea placeholder='optional medical notes' className='border p-2 w-full' onChange={e=>setForm({...form,medicalNotes:e.target.value})}/><button onClick={submit} className='bg-black text-white p-3 rounded'>Save and continue</button>{err&&<p>{err}</p>}</main>}
